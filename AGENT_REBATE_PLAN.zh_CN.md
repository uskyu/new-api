# 代理返利系统实施方案

## 1. 背景

当前系统已有邀请码、邀请关系和邀请额度字段，但实际逻辑仍是“注册即奖励”，与业务需要的“被邀请用户充值后，邀请人按比例长期返利”不一致。

本方案用于一次性落地以下能力：

- 长期返利：下级每次充值，上级都可按比例获得返利
- 返利按支付金额计算，不按实际到账额度计算
- 仅纳入易支付成功回调和管理员补单，不纳入订阅
- 支持代理分组、分组比例、代理自定义比例
- 支持代理专属推广链接/渠道
- 支持用户端代理中心
- 支持管理端代理管理、返利明细、名下用户行为查看

## 2. 本期边界

### 2.1 纳入范围

- 易支付成功回调结算返利
- 管理员补单结算返利
- 注册时绑定代理关系和推广链接来源
- 单级代理关系
- 代理分组和单独比例覆盖
- 推广链接/渠道维度统计
- 代理返利金额账本和返利流水
- 用户端代理中心页面
- 管理端代理管理与统计页面

### 2.2 暂不纳入

- 多级分佣
- 订阅订单返利
- Stripe、Creem、Waffo 等其他支付渠道返利
- 提现流程
- 财务审核流

## 3. 核心业务规则

### 3.1 返利口径

- 主口径按支付金额计算
- 返利公式：`返利金额 = 支付金额 x 返利比例`
- 落库时同时记录支付金额、返利比例、返利金额
- 如需后续支持“划转为额度”，应在划转时按当时规则换算，不直接以额度作为主账本

### 3.2 代理关系

- 用户注册时通过推广链接或邀请码建立单级代理关系
- 一个普通用户仅归属于一个上级代理
- 一个代理可拥有多个推广链接，用于区分不同渠道来源

### 3.3 比例优先级

- 代理个人自定义比例优先
- 若代理未设置自定义比例，则使用所属代理分组比例
- 若代理未绑定分组，则回退到系统默认比例或禁止成为代理

### 3.4 返利结算时机

- 易支付回调成功时结算返利
- 管理员补单成功时结算返利
- 同一笔充值只能结算一次返利

### 3.5 账本原则

- 返利必须有独立金额账本，不继续复用 `aff_quota/aff_history_quota`
- 返利流水必须可追溯、可对账、可防重复
- 如未来支持提现、冻结、扣回，均基于独立返利账本扩展

## 4. 现有实现与改造判断

### 4.1 现有可复用部分

- 用户表已有邀请关系字段：`inviter_id`
- 用户表已有邀请码字段：`aff_code`
- 易支付回调入口已存在
- 管理员补单入口已存在
- 管理端已有充值记录和日志基础能力

### 4.2 现有不足

- 当前返利逻辑是注册即送额度，不是充值返利
- 现有邀请额度字段是额度账本，不适合金额返利
- 没有返利流水表
- 没有代理分组模型
- 没有推广链接/渠道模型
- 没有按代理名下用户过滤的充值和日志查询接口

## 5. 数据模型设计

建议一次性按以下结构建模，避免二次返工。

### 5.1 新增表：`agent_rebate_groups`

用途：管理代理分组和默认返利比例。

建议字段：

- `id`
- `name`：分组名称
- `rebate_rate`：返利比例，建议十进制定点存储
- `status`：启用/禁用
- `remark`
- `created_at`
- `updated_at`

建议索引：

- `name` 唯一索引
- `status` 普通索引

### 5.2 新增表：`agent_profiles`

用途：标记某用户是否为代理，以及代理个性化配置。

建议字段：

- `id`
- `user_id`
- `status`：启用/禁用
- `rebate_group_id`
- `custom_rate`：可空，非空时覆盖分组比例
- `rebate_balance_amount`：可用返利余额
- `rebate_total_amount`：累计返利金额
- `remark`
- `created_at`
- `updated_at`

建议索引：

- `user_id` 唯一索引
- `rebate_group_id` 普通索引
- `status` 普通索引

### 5.3 新增表：`agent_promo_links`

用途：一个代理多个推广渠道链接。

建议字段：

- `id`
- `agent_user_id`
- `name`：渠道名称，如“微信社群”“官网Banner”
- `code`：推广码，注册时用于归因
- `status`
- `landing_page`：跳转地址，可空
- `created_at`
- `updated_at`

建议索引：

- `code` 唯一索引
- `agent_user_id` 普通索引

### 5.4 新增表：`agent_rebate_records`

用途：每笔返利流水与幂等控制核心表。

建议字段：

- `id`
- `topup_id`
- `trade_no`
- `source_type`：`epay` / `manual`
- `invitee_user_id`
- `agent_user_id`
- `promo_link_id`：可空，但建议落库
- `pay_amount`
- `rebate_rate`
- `rebate_amount`
- `status`：已结算/已取消/已回滚
- `created_at`
- `settled_at`
- `remark`

建议索引与约束：

- `topup_id` 唯一索引，或 `trade_no + source_type + agent_user_id` 唯一索引
- `agent_user_id` 普通索引
- `invitee_user_id` 普通索引
- `promo_link_id` 普通索引
- `created_at` 普通索引

### 5.5 用户表建议新增字段

建议在 `user` 表新增：

- `promo_link_id`：用户注册来源推广链接 ID

说明：

- `inviter_id` 仍保留，用于表达上级用户关系
- `promo_link_id` 用于渠道维度统计
- 这样既能看“谁邀请了我”，也能看“我是从哪个链接来的”

## 6. 后端设计

### 6.1 注册绑定逻辑

- 注册参数由原邀请码兼容升级为推广码解析
- 若传入的是代理推广链接码：
  - 查到 `agent_promo_links`
  - 绑定 `inviter_id = agent_user_id`
  - 绑定 `promo_link_id`
- 若传入的是旧邀请码：
  - 兼容解析为 `inviter_id`
  - `promo_link_id` 可为空
- 原“注册即给邀请人奖励额度”的逻辑移除

### 6.2 返利结算服务

建议新增统一服务，例如：

- `service/agent_rebate.go`

核心职责：

- 根据充值记录找到充值用户
- 判断该用户是否存在 `inviter_id`
- 判断上级是否为启用中的代理
- 计算有效返利比例
- 生成返利流水
- 增加代理返利余额和累计返利
- 保证事务一致性和幂等

### 6.3 结算入口改造

一期仅改以下入口：

- 易支付成功回调
- 管理员补单成功逻辑

要求：

- 充值成功与返利结算放在同一事务链路内或通过可靠幂等方式衔接
- 即使重复回调，也不能重复返利

### 6.4 管理端查询接口

建议新增或扩展以下接口：

- 代理分组 CRUD
- 代理资料 CRUD
- 推广链接 CRUD
- 代理返利流水列表
- 代理汇总统计
- 某代理名下用户列表
- 某代理名下用户充值记录列表
- 某代理名下用户消费日志列表

### 6.5 用户端查询接口

建议新增：

- 我的代理中心首页数据
- 我的推广链接列表
- 我的返利流水列表
- 我的下级用户列表
- 我的渠道统计数据
- 我的返利余额划转接口（若本期一起做）

## 7. 前端设计

### 7.1 用户端：代理中心

建议新建独立页面，不继续混在当前钱包邀请卡片中。

页面内容建议包括：

- 当前返利余额
- 累计返利
- 今日返利
- 本月返利
- 推广链接列表
- 渠道维度数据：注册人数、充值金额、返利金额
- 名下用户列表
- 返利流水列表

### 7.2 管理端：代理管理

建议新建代理管理模块，至少包括：

- 代理列表
- 分组管理
- 代理资料编辑
- 推广链接管理
- 返利流水查看
- 代理统计看板
- 名下用户明细
- 名下用户充值记录
- 名下用户消费日志入口

### 7.3 旧页面处理

- 当前钱包页中的“邀请奖励”区域需要调整定位
- 若保留，应作为代理中心入口或简版概览
- 文案需从“邀请奖励/邀请额度”迁移为“代理返利/返利余额”等新口径

## 8. 推荐执行顺序

虽然目标是一步到位上线，但开发顺序仍建议按模块推进。

### 8.0 初始化策略

采用“自动迁移结构 + 管理端手动初始化业务”的双阶段方案。

- 应用启动时由主节点自动完成数据库结构迁移
- 管理员左侧导航新增 `代理` 模块
- 首次进入代理页面时展示初始化引导页
- 点击 `初始化代理功能` 后，仅执行业务初始化，不执行建表和改表

`初始化代理功能` 建议执行内容：

- 创建默认代理分组
- 写入代理相关系统配置
- 标记代理系统已初始化
- 启用代理功能开关

不建议通过页面按钮执行数据库迁移，原因包括：

- 多节点环境下执行节点不确定
- 数据库权限和运行时状态不可控
- 迁移失败后页面状态难以回滚
- 不符合现有项目自动迁移机制

### 阶段 A：数据结构与后端基础

- 新增代理相关表
- 为用户表新增 `promo_link_id`
- 编写迁移逻辑，兼容 SQLite / MySQL / PostgreSQL
- 定义 GORM 模型与基础查询方法

### 阶段 B：注册归因与代理配置

- 改造注册逻辑，支持推广链接归因
- 管理端实现代理分组、代理资料、推广链接配置接口
- 兼容旧邀请码逻辑

### 阶段 C：返利结算

- 实现统一返利结算服务
- 接入易支付成功回调
- 接入管理员补单成功逻辑
- 补充日志记录与幂等控制

### 阶段 D：查询与统计接口

- 用户端代理中心数据接口
- 管理端代理维度统计接口
- 代理名下用户、充值、日志查询接口

### 阶段 E：前端页面

- 用户端代理中心
- 管理端代理管理模块
- 钱包页入口改造

### 阶段 F：联调与验证

- 回调幂等验证
- 补单重复执行验证
- 不同数据库兼容验证
- 分组比例与代理自定义比例优先级验证
- 渠道归因验证

## 9. 风险点与注意事项

### 9.1 幂等风险

- 易支付回调可能重复触发
- 管理员补单可能重复操作
- 必须依赖返利流水唯一约束和事务控制防止重复返利

### 9.2 金额精度

- 返利比例和返利金额建议使用十进制定点处理
- 避免直接使用浮点数进行最终结算

### 9.3 兼容性

- 数据库需兼容 SQLite、MySQL、PostgreSQL
- 迁移脚本不要使用单一数据库专属语法
- 本地开发可优先使用 SQLite，但上线前必须使用生产同类型数据库再验证一次迁移与查询行为

### 9.4 旧逻辑迁移

- 需移除注册即送邀请奖励的逻辑
- 需评估旧 `aff_quota/aff_history_quota` 是否保留为历史字段
- 若保留，应明确它们不再承担代理返利主账本职责
- 远程已有数据时，第一版只做“新增表/新增字段/新增逻辑”，不删除旧字段，不自动回填历史返利

### 9.5 查询性能

- 管理端代理统计会涉及用户、充值、返利流水、日志联查
- 需要提前设计索引，避免后期页面过慢

## 10. 测试建议

### 10.1 后端测试重点

- 注册归因正确性
- 代理分组与自定义比例优先级
- 易支付回调返利成功
- 易支付重复回调不重复返利
- 管理员补单返利成功
- 管理员重复补单不重复返利
- 无上级、上级非代理、代理禁用等边界场景

### 10.2 前端联调重点

- 用户端代理中心数据正确
- 管理端统计与明细一致
- 推广链接复制和注册归因正确
- 分页、筛选、搜索体验正常

### 10.3 升级测试重点

- 使用已有历史数据的旧库升级到新版本后可正常启动
- 自动迁移后旧用户、旧充值记录、旧邀请码逻辑不受影响
- 代理初始化接口重复执行时具备幂等性
- 管理员未初始化代理功能前，代理页面仅展示引导状态

## 11. 部署策略

### 11.1 本地开发

- 本地先用 SQLite 完成日常开发和快速回归
- 增加旧库升级测试样本，验证平滑迁移
- 在联调前再使用 MySQL 或 PostgreSQL 进行一次完整数据库兼容测试

### 11.2 远程部署

- 先备份数据库
- 部署包含新表和新字段的新版本
- 由主节点启动并完成自动迁移
- 确认结构升级成功后，管理员进入代理页面点击初始化
- 初始化完成后再正式启用代理功能

### 11.3 多节点注意事项

- 只有主节点负责执行迁移
- 其他节点应在主节点迁移成功后再滚动升级
- 避免出现代码已依赖新表但数据库尚未升级完成的情况

## 12. 建议的最终落地策略

本项目建议采用“一次建模到位、一次开发到位、按模块推进上线前联调”的策略。

推荐实施原则：

- 金额返利账本独立
- 返利流水可追溯
- 单级代理先落地
- 渠道归因本期一起完成
- 所有返利从统一服务结算，不在控制器中分散写业务

## 13. 开始执行前的确认项

执行前建议再确认以下产品口径：

- 代理返利余额是否支持划转为站内额度
- 代理返利余额是否允许管理员手工调整
- 推广链接是否允许停用后继续保留历史归因
- 历史已完成易支付订单是否需要补算返利
- 管理端是否需要导出代理返利明细

---

确认以上方案后，可直接进入实施阶段，建议从“表结构 + 模型 + 统一返利结算服务”开始。
